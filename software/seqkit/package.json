{
  "name": "@platforma-open/milaboratories.software-binary-collection.software-seqkit",
  "version": "1.1.0",
  "description": "SeqKit FASTA/Q toolkit package for Platforma Backend",
  "scripts": {
    "cleanup": "rm -rf ./pkg-*.tgz && rm -rf ./dist/",
    "build": "pl-pkg build --all-platforms",
    "prepublishOnly": "pl-pkg prepublish"
  },
  "files": [
    "dist/"
  ],
  "block-software": {
    "entrypoints": {
      "main": {
        "docker": {
          "artifact": {
            "type": "docker",
            "dockerfile": "Dockerfile",
            "context": "./linux-x64/"
          },
          "cmd": [
            "/app/bin/seqkit"
          ]
        },
        "binary": {
          "artifact": {
            "registry": "platforma-open",
            "type": "binary",
            "roots": {
              "linux-x64": "./linux-x64",
              "linux-aarch64": "./linux-aarch64",
              "macosx-x64": "./macosx-x64",
              "macosx-aarch64": "./macosx-aarch64",
              "windows-x64": "./windows-x64"
            }
          },
          "cmd": [
            "{pkg}/bin/seqkit"
          ]
        }
      }
    }
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@platforma-sdk/package-builder": "catalog:"
  }
}
